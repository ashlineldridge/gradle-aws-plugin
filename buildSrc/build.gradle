apply plugin: 'scala'
apply plugin: 'maven'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
	compile 'org.scala-lang:scala-library:2.11.8'
    compile 'com.amazonaws:aws-java-sdk-cloudformation:1.11.0'
}
